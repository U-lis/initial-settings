# 이 레포는 셸 스크립트다. 예전 .gitignore 는 파이썬 패키징 템플릿이라
# lib/ 와 lib64/ 가 들어 있었고, 그 때문에 lib/common.sh 가 조용히 커밋에서
# 빠졌다. 필요한 것만 남긴다.

# 생성물 (jetbrains/build 등)
build/

# 에디터 / OS
.idea/
.vscode/
*~
*.swp
.DS_Store

# 파이썬 (jetbrains/ 의 생성 스크립트)
__pycache__/
*.py[cod]
